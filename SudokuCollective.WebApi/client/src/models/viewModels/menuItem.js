class MenuItem {

    constructor(url, text, mdiIcon, isSitePage, isLoggedIn) {

        this.url = url,
        this.text = text,
        this.mdiIcon = mdiIcon,
        this.isSitePage = isSitePage,
        this.isLoggedIn = isLoggedIn
    }
}

export default MenuItem;